= render("shared/bootstrap/messages")
%form.form-horizontal
  #model{:"data-hash"=>model.to_json, :"data-authenticity-token"=>form_authenticity_token}
  %div{"data-bind" => "with: $data.section(formRoot,'model')"}
    = yield :inner_html
